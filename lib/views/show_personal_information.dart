import 'package:flutter/material.dart';

class ShowPersonalInformation extends StatelessWidget {
  const ShowPersonalInformation({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Container(
        color: Colors.lightBlue,
      ),
    );
  }
}
